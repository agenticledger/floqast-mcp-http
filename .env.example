# Server port (default: 3100)
PORT=3100

# No API keys needed — clients pass their own FloQast credentials via Bearer token
# Bearer token format: <api-key> or <api-key>:<region>
# Region defaults to 'us' if not specified (options: us, eu, au)
